// alert("Hello from Rails")

// we're in rails stack so access to instance variables
// $("div.comments").append('<%= @comments.first.content %>')

// still need to return js valid objects

var html = "<%= j(render('comments/comments')) %>"

$("div.comments").append('html')

respond_to do |format|
  format.html {render 'index.html', :layout => false}
  format.js {render 'index.js'}
end
