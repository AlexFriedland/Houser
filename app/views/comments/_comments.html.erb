<div class="comments">
  <ol>
    <% if @comments.first.body != "" || @comments.first.body != nil %>
      <% @comments.each do |comment| %>
        <li><%= comment.body %></li>
      <% end %>
    <% end %>
  </ol>
</div>
