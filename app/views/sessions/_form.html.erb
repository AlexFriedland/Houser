<%= form_for @user do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user_id.errors.count, "error") %> prohibited this year from being saved:
      </h2>
      
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.text_field :email, :placeholder => "email:" %>
  <%= f.password_field :password, :placeholder => "password:" %>
  <%= f.submit %>
<% end %>
